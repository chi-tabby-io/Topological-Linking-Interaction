// This script is just so that if need-be I will add everything to a separate
// file from index.html

function init() {
    var scene = new THREE.Scene();

    var camera = new THREE.PerspectiveCamera()
}